export interface Idraggable {
    isDragging: boolean,
    originalX: number,
    originalY: number,
    translateX: number,
    translateY: number,
    lastTranslateX: number,
    lastTranslateY: number
}